<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jtw.jtwback.domain.conn.dao.ConnDao">

    <select id="selectDbDetail">
        SELECT
            *
        FROM
            TB_ACCESS
        WHERE
            ACCS_NM = #{db}
    </select>
</mapper>